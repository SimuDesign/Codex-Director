# 更新说明

[English changelog](CHANGELOG.md)

## 1.3.1

本条目记录本地 UI 修复，不代表已经创建 Tag 或发布 GitHub Release。应用显示版本为 `1.3.1`，内部构建号为 `26`。

- 统一能力文件夹分组标题、操作、卡片和清单的左右基线，明确标题、Tab、搜索与内容之间的垂直间距。
- 文件夹 Tab、添加能力筛选和设置主题选择统一为真正的原生分段控件与品牌描边，不再沿用系统蓝色填充。
- 统一文件夹搜索与排序的实际绘制高度，以及图标菜单的描边和点击区域，保留原有搜索、状态、排序、详情和成员操作。
- 保留全部已有文件夹成员、配套关系、能力源文件、缓存/索引行为及能力包 manifest v1。

## 1.3.0

本条目记录源码合流内容，不代表已经创建 Tag 或发布 GitHub Release。应用显示版本为 `1.3.0`，内部构建号为 `25`。

- 以“能力文件夹”替换未发布的自动能力分类：支持全局、项目与自定义文件夹；同一能力可加入多个文件夹。
- 支持新建、改名、删除、排序、全局搜索、文件夹内搜索，以及多选添加已有 Agent / Skill。仅整理本地偏好，不修改能力源文件。
- 文件夹内部提供“Agent 与配套 Skill → Agent → Skill”三个 Tab；每个窗口会话分别保留搜索、排序、展开与详情状态。
- 配套关系仅来自注册表、Agent TOML / Brief 或 Skill 描述中的明确声明；否定指令和歧义引用不建立关系。文件夹外或全局关联仅作预览，不计数、不自动加入。
- Agent / Skill 详情支持双向查看关联；配套声明与近七天去重同会话证据分开展示。共同出现不证明 Agent 调用了 Skill，也不证明能力有效。
- 优化响应式 4 / 3 / 2 / 1 文件夹网格、紧凑描边关系分组、三 Tab、排序菜单和原位详情侧栏。
- 保留已有“自我训练”及其他文件夹的全部成员；新安装只预建一个空的“自我训练”，不恢复自动填充。
- 修复原生搜索框在清除搜索或切换 Tab 时的布局循环：每种响应式布局只创建一个实际文本编辑器。
- 修复源刷新已在进行时展示时钟忽略较短轮询间隔的问题；正常自动刷新频率保持不变。
- 修复旧周重置周期快照被重复计入每日消耗的问题。曾产生 `224%` 的回归序列现在得到 `62%`；不以强制截断为 100% 掩盖真实跨周期消耗。
- 补充中英文说明、合成能力文件夹截图与回归测试。能力包 manifest v1、历史会话解析器版本均保持不变。

## 1.2.0

- 新增本地安全恢复：隔离验证能力包、手动映射项目、预检冲突、最终确认后只创建缺失文件。
- 不覆盖或合并已有文件，不执行包内脚本，不安装插件或依赖，不联网。
- 失败清理与会话内撤销只移动已验证且未变化的新增对象到私有隔离区，保留用户检查与手动删除的机会。

## 1.1.1

- 首页当前五小时与周额度使用菜单栏同一份脱敏实时账户快照，七日柱状图继续只使用同来源索引历史。
- 首页刷新在用户启用账户额度功能时同时更新账户数据；关闭后不启动账户读取进程。

## 1.1.0

- 新增五小时与周额度同心双环、独立重置时间及过期处理。
- 菜单栏双额度显示 `5h 82% w 57%`，单额度只显示百分比。

## 1.0.0

- 建立隐私审计过的公共开源基线，统一设置按钮尺寸和简洁版本显示。
- 提供源码构建及未公证、ad-hoc 签名社区构建路径；不声称经过 Apple 公证。

更早的完整公共版本记录见 [English changelog](CHANGELOG.md)。私人开发历史不进入公共更新说明。
