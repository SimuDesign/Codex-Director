# Codex Director 1.3.0 Finalization Implementation Plan

> Execute in reviewable batches using the existing project verification and PR/CI workflow.

**Goal:** Finish and merge the approved 1.3.0 Capability Folders work, UI refinement, quota correction, and bilingual public documentation without creating a tag or release.

**Architecture:** Preserve the existing Core projections, stable IDs, local folder preferences, and manifest v1. Exercise the shipped views in the disposable Debug Validation Host with synthetic data and in-memory preferences; production data and the private archive remain untouched.

**Tech Stack:** Swift 6, SwiftUI/AppKit, SQLite, ZIPFoundation 0.9.20, GitHub CLI and existing CI.

---

## Task 1: Freeze and review the candidate

- Review the existing folder UI, detail ordering, local layout/color tokens, bilingual strings, quota replay correction, and tests.
- Preserve all saved custom-folder memberships; do not seed, clear, or migrate production preferences.
- Run focused folder, relationship, theme, refresh, localization, and quota regressions.
- Build the isolated host with `./scripts/build-ui-validation.sh`; never run its fixtures against the installed application's storage.

## Task 2: Verify core journeys and collect public evidence

- Exercise entry/folder search, all three tabs, companion expansion and reverse detail links, staged existing-capability import, theme switching, and refresh loading/recovery.
- Check Chinese/English and Light/Dark at 720×480 and 1280×800; inspect keyboard and accessibility semantics and record any evidence limits honestly.
- Capture only synthetic Capability Folders entry and companion-browser screenshots. Inspect content and image metadata, then register final hashes in `docs/public-asset-inventory.md`.
- Keep temporary databases, logs, packages, and build artifacts outside tracked files.

## Task 3: Finish public documentation

- Update `README.md`, `README.zh-CN.md`, `CHANGELOG.md`, and Chinese version notes with the final 1.3.0 scope and screenshot links.
- Document new-install empty Self Training versus upgrade membership preservation, preview-only companions, separate co-observation evidence, and the directional reset correction.
- Record fresh verification in `docs/design/2026-09-17-1.3-finalization-quality.md` without personal paths or production data.
- Keep visible version 1.3.0 and build 25; no package-format, parser, schema, or dependency upgrade.

## Task 4: Validate and submit

- Run `git diff --check`, `./scripts/verify.sh`, and the Release build/bundle verifier.
- Stage only the reviewed 1.3.0 files and retained licensed screenshots. Commit using the configured SimuDesign noreply identity.
- Require a clean tree and `./scripts/audit-public-release.sh --all` before public push. Audit the final commit and complete public history.
- Push `codex/capability-folders-1.3.0`, create a PR into `main`, wait for required CI, and squash merge only if all required checks pass.
- Fetch the merged commit and fast-forward local `main` without discarding work. Do not bypass branch protection, force push, create tags, or publish a GitHub Release.

## Acceptance

- Core synthetic journeys, fresh tests, public audit, Release identity/signature/universal architectures, and required CI pass.
- Existing local folder preferences remain unchanged.
- Public README and version notes are bilingual and screenshot media are licensed, synthetic, reviewed, and registered.
- The approved work is merged into public `main`; tag and release remain separately authorized actions.
